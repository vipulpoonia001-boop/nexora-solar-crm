# Production API base URL.
# If frontend and backend are served on same domain with Nginx /api proxy, use:
# VITE_API_URL=/api
#
# If backend is on separate domain, use full URL:
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=/api
